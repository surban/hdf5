matrix:
  include:
    - os: linux
      dist: trusty
#    - os: osx
#      osx_image: xcode9.3
language: c
before_install:
before_install:
  - if [ "$TRAVIS_OS_NAME" = "linux" ]; then wget -O cmake.sh https://cmake.org/files/v3.11/cmake-3.11.1-Linux-x86_64.sh; sudo sh cmake.sh --skip-license --exclude-subdir --prefix=/usr/local ; fi
  - if [ "$TRAVIS_OS_NAME" = "linux" ]; then sudo apt-get install zlib1g-dev ; fi
#  - if [ "$TRAVIS_OS_NAME" = "osx" ]; then sudo rm '/usr/local/include/c++' ; brew update ; brew install hdf5 ; fi
script: 
  - bash scripts/build_linux.sh
after_success:
  - zip --symlinks hdf5_linux.zip build_linux/bin/*.so*
deploy:
  provider: releases
  api_key: $GITHUB_TOEN
  file: hdf5_linux.zip
  skip_cleanup: true
  on:
    tags: true
